version: '3.8'

services:
  nizhal-ml-backend:
    build: 
      context: ./ml
      dockerfile: Dockerfile
    container_name: nizhal-ml-backend
    ports:
      - "8000:8000"
    env_file:
      - ./ml/.env
    restart: unless-stopped
